import Link from "next/link"
export function Panel({ title, children }: { title: string; children: React.ReactNode }) { return <section className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-lg font-semibold text-slate-900">{title}</h2><div className="mt-4 text-sm leading-6 text-slate-600">{children}</div></section> }
export function Empty({ children }: { children: React.ReactNode }) { return <p className="rounded-lg border border-dashed border-slate-300 bg-slate-50 p-4 text-sm leading-6 text-slate-600">{children}</p> }
export function Tag({ children }: { children: React.ReactNode }) { return <span className="inline-flex rounded-md bg-slate-100 px-2 py-1 text-xs font-medium text-slate-700">{children}</span> }
export function Table({ headings, rows }: { headings: string[]; rows: React.ReactNode[][] }) { return <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b border-slate-200 text-xs uppercase tracking-wide text-slate-500"><tr>{headings.map(x=><th key={x} className="whitespace-nowrap px-3 py-3 font-medium">{x}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr key={i} className="border-b border-slate-100 align-top">{row.map((x,j)=><td key={j} className="max-w-sm px-3 py-3">{x ?? "Unavailable"}</td>)}</tr>)}</tbody></table>{rows.length===0 && <Empty>No observed records are available for this view.</Empty>}</div> }
export function PrivateLink({ href, children }: { href: string; children: React.ReactNode }) { return <Link href={href} className="font-medium text-teal-700 underline decoration-teal-300 underline-offset-4">{children}</Link> }
export const shown = (value: string | number | boolean | null | undefined) => value === null || value === undefined ? "Unavailable" : String(value)
