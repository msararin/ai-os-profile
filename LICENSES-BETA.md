# Invited beta license boundary

The owner authorized deployment and two numeric-code users, and preapproved implementation steps until deployment finishes. This records that authorization; it does not claim a separate legal acknowledgement or upstream permission.

The installed vachanatts 0.0.7 runtime has unresolved license evidence: the wheel, PyPI page and upstream VYNCX/VachanaTTS2 repository do not provide a clear package license. The Thai model declares MIT, which does not automatically license runtime code. The public library inventory discloses this gap.

The runtime executes only on the owner's Mac. This website repository and Vercel deployment redistribute neither the runtime nor its models. Only audio generated at the request of the two invited code users is delivered. No unrestricted availability, legal clearance or fully open-source stack is claimed.

If upstream declines this use, disable the Thai runtime and replace it with a verified licensed implementation before restoring Thai audio. Resolve the license evidence before broader release or package distribution.
